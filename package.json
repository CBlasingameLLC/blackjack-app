{
  "name": "blackjack-app",
  "version": "1.0.0",
  "private": true,
  "description": "6-deck S17 blackjack simulator with Hi-Lo counting, basic strategy grading, and Illustrious 18 deviation training.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap open ios",
    "cap:android": "npx cap open android",
    "build:cap": "vite build && npx cap sync"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  }
}
